# Hello!
To run this, just run this command:
```
curl -sL https://raw.githubusercontent.com/boopidoopiloopi/cursor-converter/main/installer.sh -o i.sh && bash i.sh; rm i.sh
```
It also adds the cursor converter into the app menu :3
🐱️🐱️
