# Hello!
To run this, just run this command:
```
curl -O https://raw.githubusercontent.com/boopidoopiloopi/cursor-converter/main/installer.sh && chmod +x installer.sh && ./installer.sh && echo -e "\nВсем приветы чизеты!\nСкрипт короче скачал ГОЛЫЙ (воу) репозиторий, и сделал так чтобы можно было запускать main.py\n" && ./BoopiCursorConverter/main.py >/dev/null 2>&1 &; disown; echo -e "\nНу все, чтобы запустить, входишь в папку BoopiCursorConverter и запускаешь main.py\nУдачи!"
```
Then run main.py in the new repository
